import { BrowserRouter, Routes, Route } from 'react-router-dom';

export default function App() {
  return (
    <BrowserRouter basename="/minuan-app-store">
      <Routes>
      </Routes>
    </BrowserRouter>
  );
}
